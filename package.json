{
  "name": "b-me-wellness",
  "version": "1.0.0",
  "description": "Mental wellness multi-agent chatbot with adaptive conversational frames",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json --watch",
    "lint": "eslint ."
  },
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {
    "@sveltejs/adapter-vercel": "^5.3.1",
    "@sveltejs/kit": "^2.5.10",
    "@sveltejs/vite-plugin-svelte": "^3.0.2",
    "eslint": "^9.0.0",
    "eslint-plugin-svelte": "^2.35.1",
    "prettier": "^3.1.1",
    "prettier-plugin-svelte": "^3.1.2",
    "svelte": "^4.2.18",
    "svelte-check": "^3.6.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@google/genai": "^0.2.0",
    "sentiment": "^5.0.2"
  }
}
